<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
	<nav>
		<% if logged_in? %>
			<a href="/cars/new"> Add A Car To Your Collection </a><br><br>
			<a href="/logout"> LOG OUT </a>
			<p id='user-id'> Logged In As: <%= current_user.username %></p>
		<% else %>
			<a href='/signup'> Creat A User </a>
			<a href='/login'> LOG IN </a>
		<% end %>
	</nav>

	<div class='container'>
		<h1><a href="/">Online Car Store</a></h1>
	</div>

	<div class='container'>
		<% if !@error_message.nil? %>
		<div class="alert">
			<%= @error_message %>
		<div>
		<% end %>
	</div>

	<div class="container">
		<%= yield %>
	</div>

</body>
</html>
